name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-workflows:
    name: Workflow lint (actionlint + zizmor)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with: { persist-credentials: "false" }
      - uses: taiki-e/install-action@4684b8405694ae9dd42c9f39ba901a70ae83f4a3 # v2.82.9
        with: { tool: zizmor }
      - name: Install actionlint (pinned, checksum-verified, no root)
        run: |
          set -euo pipefail
          V=1.7.12
          curl -fsSLo actionlint.tar.gz "https://github.com/rhysd/actionlint/releases/download/v${V}/actionlint_${V}_linux_amd64.tar.gz"
          curl -fsSLo checksums.txt "https://github.com/rhysd/actionlint/releases/download/v${V}/actionlint_${V}_checksums.txt"
          grep "actionlint_${V}_linux_amd64.tar.gz" checksums.txt | sha256sum -c -
          tar xzf actionlint.tar.gz actionlint
          mkdir -p "$HOME/.local/bin"
          install actionlint "$HOME/.local/bin/actionlint"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - run: actionlint
      - run: zizmor .github/workflows/
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  lint-go:
    name: Go Lint
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with: { persist-credentials: "false" }
      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version-file: go.mod
          # std-lib-only module: no go.sum for the cache key, so caching is off.
          cache: false
      - name: Install golangci-lint
        run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.11.4
      - run: golangci-lint run ./...

  test-go:
    name: Go Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [lint-go]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with: { persist-credentials: "false" }
      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version-file: go.mod
          cache: false
      - run: go test ./... -race -count=1

  vuln-scan:
    name: Vulnerability Scan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [test-go]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with: { persist-credentials: "false" }
      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version-file: go.mod
          cache: false
      - name: Install govulncheck
        # Pinned: the private-repo review flagged @latest as drift-prone.
        run: go install golang.org/x/vuln/cmd/govulncheck@v1.1.4
      - run: govulncheck ./...

  summary:
    name: Summary
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [lint-workflows, lint-go, test-go, vuln-scan]
    if: always()
    env:
      R_LINTWF: ${{ needs.lint-workflows.result }}
      R_LINT: ${{ needs.lint-go.result }}
      R_TEST: ${{ needs.test-go.result }}
      R_VULN: ${{ needs.vuln-scan.result }}
    steps:
      - name: Render summary
        run: |
          {
            echo "### Go CI (hosted)"
            echo ""
            echo "| Stage | Result |"
            echo "|-------|--------|"
            echo "| Workflow lint | $R_LINTWF |"
            echo "| Go Lint | $R_LINT |"
            echo "| Go Test | $R_TEST |"
            echo "| Vulnerability Scan | $R_VULN |"
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Fail if any stage failed
        run: |
          for r in "$R_LINTWF" "$R_LINT" "$R_TEST" "$R_VULN"; do
            [ "$r" = "success" ] || { echo "::error::a stage did not succeed ($r)"; exit 1; }
          done
